<% if notice.present? %>
  <div class="alert alert-success" role="alert" id="notice">
    <%= notice %>
  </div>
<% end %>

<% if alert.present? %>
  <div class="alert alert-danger" role="alert" id="notice">
    <%= alert %>
  </div>
<% end %>

<div class="mt-2 p-5 bg-light border rounded-3">
  <h1><%= MiningType.model_name.human(count: @mining_types.count) %></h1>

  <table class="table table-hover">
    <thead>
      <tr>
        <th><%= @mining_types.human_attribute_name(:description) %></th>
        <th><%= @mining_types.human_attribute_name(:acronym) %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @mining_types.each do |mining_type| %>
        <tr>
          <td><%= mining_type.description %></td>
          <td><%= mining_type.acronym %></td>
          <td><%= link_to t('links.show'), mining_type %></td>
          <td><%= link_to t('links.edit'), edit_mining_type_path(mining_type) %></td>
          <td><%= link_to t('links.delete'), mining_type, method: :delete, data: { confirm: t('messages.confirm') } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to t('new.mining_type'), new_mining_type_path, class:"btn btn-primary" %>

  <%= link_to t('links.back'), :root, class:"btn btn-secondary" %>

  <div class="container text-center">
    <%= will_paginate @mining_types, class:"apple_pagination", previous_label: t('previous'), next_label: t('next') %>
  </div>
</div>